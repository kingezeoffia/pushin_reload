name: pushin_reload
description: PUSHIN MVP - iOS-first, Android-ready Flutter app for content blocking with workout-based unlock
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.24.0

